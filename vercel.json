{
    "builds": [{
        "src": "startup/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python4.0" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "startup/wsgi.py"
        }
    ]
}
